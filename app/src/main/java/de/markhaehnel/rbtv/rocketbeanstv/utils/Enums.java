package de.markhaehnel.rbtv.rocketbeanstv.utils;

public class Enums {
    public enum ChatState {
        HIDDEN,
        OVERLAY,
        FIXED
    }

    public enum Quality {
        CHUNKED,
        HIGH,
        MEDIUM,
        LOW,
        MOBILE
    }

    public enum EventStatus {
        OK,
        FAILED
    }
}
