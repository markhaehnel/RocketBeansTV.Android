package de.markhaehnel.rbtv.rocketbeanstv.utils

class Enums {
    enum class ChatState {
        HIDDEN,
        OVERLAY,
        FIXED
    }

    enum class EventStatus {
        OK,
        FAILED
    }
}
